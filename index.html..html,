<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Sorry Moupia</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    background: radial-gradient(circle at top, #1a2a6c, #000);
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    perspective: 1200px;
    overflow: hidden;
    color: white;
  }

  .scene {
    transform-style: preserve-3d;
    animation: rotateScene 10s infinite linear;
    margin-bottom: 40px;
  }

  .text3d {
    font-family: 'Arial Black', sans-serif;
    font-size: 80px;
    position: relative;
    transform-style: preserve-3d;
    text-shadow: 0 0 25px rgba(0, 150, 255, 0.7);
  }

  .text3d::before,
  .text3d::after {
    content: "I AM SORRY";
    position: absolute;
    left: 0;
    top: 0;
  }

  .text3d::before {
    transform: translateZ(-20px);
    color: #0b4f6c;
  }

  .text3d::after {
    transform: translateZ(-40px);
    color: #06283d;
  }

  @keyframes rotateScene {
    0% { transform: rotateY(0deg); }
    100% { transform: rotateY(360deg); }
  }

  .lines {
    font-family: 'Georgia', serif;
    font-size: 22px;
    text-align: center;
    opacity: 0;
    animation: fadeIn 2s forwards;
  }

  .line1 { animation-delay: 2s; }
  .line2 { animation-delay: 5s; }
  .line3 { animation-delay: 8s; }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .name {
    font-size: 32px;
    margin-top: 20px;
    color: #8fd3f4;
    font-weight: bold;
    letter-spacing: 2px;
  }
</style>
</head>

<body>

  <div class="scene">
    <div class="text3d">I AM SORRY</div>
  </div>

  <div class="lines line1">
    I never meant to hurt you,
  </div>

  <div class="lines line2">
    my words failed, but my feelings didn’t.
    sach mai bohot bura lag gaya h 
  </div>

  <div class="lines line3 name">
     
  </div>

</body>
</html>